package com.voyanta.survey.enums;

/**
 * Enum with the rough trip lengths a user can choose (from a two-day weekend up to more
 * than a week). It is used when the user does not pick exact travel dates.
 */
public enum DurationType {
    WEEKEND_2_DAYS, DAYS_3_4, DAYS_5_7, WEEK_PLUS
}